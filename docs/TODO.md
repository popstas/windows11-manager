# next
- [x] Показать скалярные настройки claudeWt/homeassistant на вкладке Claude в окне настроек
- [ ] Штамп сборки в трее не отвечает на вопрос, ради которого в него смотрят: `WM_BUILD_UNIX` вшивается на компиляции трея, а логика claude-wt лежит в node-части (`src/**`), которую трей только запускает процессом. Выкатка, тронувшая одни js-файлы, трей не пересобирает — штамп честно показывает прошлую сборку Rust, хотя выкачено новое. Поймано 2026-08-20: штамп говорил «23:11», при том что новый код работал. Чинить так, чтобы пункт отвечал про то, что действительно работает: время правки node-части или короткий sha `HEAD`, считанные на старте процесса, а не на компиляции. У ccfzf-picker этой болезни нет — там статика вшивается в бинарь.

# future
- [ ] Сделать плагин для claude/codex: скиллы проекта (`project-add` уже здесь, приехал из windows-mqtt) плюс скилл настройки — чтобы связку ставили одной командой, а не растаскивали файлы по репозиториям руками
- [ ] Добавить интерфейс http, дублирует mqtt
