# next
- [ ] Домержить и выкатить #24 (`claude-place`): пока ветка не в master и не на
      машине, просьба о раскладке никем не разбирается. С той стороны всё готово
      — ccfzf-picker 0.7.0 шлёт `claude-place` пунктами `Tile windows` /
      `Cascade windows` в меню `^K`, и на popstas-pc они сейчас видны и молчат.
      После выкатки проверить связку живьём и снять оговорку «windows11-manager
      раскладок пока не умеет» в CLAUDE.md и docs/window-layouts.md пикера.

# future
- [ ] Сделать плагин для claude/codex: скиллы проекта (`project-add` уже здесь, приехал из windows-mqtt) плюс скилл настройки — чтобы связку ставили одной командой, а не растаскивали файлы по репозиториям руками
- [ ] Добавить интерфейс http, дублирует mqtt
