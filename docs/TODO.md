# TODO

- [ ] Добавить обновление списка сессий: сейчас при запросе списка сессий из windows-mqtt видны только существующие (живые) сессии
- [ ] Восстановить потерянный слой view: `src/claude-wt/view.js` (`claudeWtSessions`), `view-helpers.js` (`buildSessionList`, `monitorNumberForBounds`), `view-helpers.test.js`, `focusWindow`/`focusWindowById` в `src/windows.js`, реэкспорт в `src/lib/index.js`. Файлов нет ни в одном коммите, но они есть в опубликованной 2.0.0 (`node_modules` у windows-mqtt)
- [ ] Мониторы не отслеживаются: проверить, что у каждой сессии записаны `desktop`, `monitor`, `x`, `y`, `width`, `height` (сейчас в слотах только `desktop` + `bounds`, поля `monitor` нет)
- [ ] Исправить `claude-wt restore`: работает неправильно. Сделать на снапшотах — с debounce ~5 минут запоминать открытые сессии, дать возможность посмотреть список окон за разные снимки. Если окна не менялись — новый снапшот не создавать
- [ ] Команды `snapshots-list` / `snapshots-restore`:
  - `snapshots-list` отдаёт снапшоты (время, кол-во сессий, список сессий)
  - `snapshots-restore last` восстанавливает последний сохранённый снапшот
  - `snapshots-restore <id>` восстанавливает все сессии снапшота с дедупликацией по уже открытым: если из 3 сессий снапшота 2 уже открыты — восстановить только одну
  - меню windows-mqtt «Claude Sessions»: последние 5 снапшотов (в названии время + кол-во сессий), на втором уровне список сессий снапшота и пункт «Восстановить всё»
