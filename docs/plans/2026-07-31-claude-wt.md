# claude-wt Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Модуль claude-wt в windows11-manager, который запоминает позицию окна Windows Terminal для каждой сессии Claude Code и возвращает окно на место при повторном входе в сессию, а после аварийной перезагрузки восстанавливает раскладку.

**Architecture:** Фоновый демон опрашивает окна раз в секунду, чистое ядро `step()` превращает пару снимков окон в новое состояние и список действий, действия исполняются существующим `placeWindowByConfig`. Состояние ключуется по id сессии Claude Code, заголовок окна служит индексом поиска (через дамп ccfzf плюс собственная история заголовков). Вся логика без I/O вынесена в `*-helpers.js` и покрыта vitest.

**Tech Stack:** Node.js 20+ ESM, vitest 3, commander 14, node-window-manager (vendored), VirtualDesktop11.exe, Tauri v2 (Rust) для трея. Bash + встроенный python3 для правки в ccfzf.

Дизайн: `docs/specs/2026-07-31-claude-wt-design.md`.

## Global Constraints

- Все новые модули — ESM (`"type": "module"`), импорты с расширением `.js`.
- Чистая логика живёт в `*-helpers.js` и не импортирует `node-window-manager`, `fs` и `child_process`. Тесты импортируют только хелперы — иначе они не запустятся вне Windows.
- Тесты лежат рядом с кодом как `*.test.js`, подхватываются шаблоном `src/**/*.test.js` из `vitest.config.js`.
- Запуск тестов: `npx vitest run <путь>` для одного файла, `npm test` для всех. Линт: `npm run lint`.
- Никаких спавнов процессов в горячем цикле. `VirtualDesktop11.exe` запускается только по событию привязки окна к сессии, не на каждом тике. Это прямое следствие оптимизации CPU от 2026-07-14: периодические спавны exe — главный источник паразитной нагрузки.
- **`getWindows()` нельзя звать на каждом тике.** Первая редакция плана разрешала это, считая, что вызов дешёвый, раз не спавнит процессы. Обоснование неверное: `getWindows()` делает `OpenProcess` + запрос пути exe + чтение заголовка на **каждое** окно системы — коммит `96c2584` в этом же репозитории измерил ~31 мс CPU на вызов, из-за чего автоплейсер на двух вызовах в секунду стоил ~6% ядра. Задача 6 должна дёшево опрашивать `getVisibleWindowIds()` (~0.6 мс, `EnumWindows` + `IsWindowVisible`), брать полный снимок только при появлении незнакомого hwnd, а заголовок и bounds читать лишь у известных окон терминала — их единицы. Чистый слой это позволяет: `step()` требует только `[{ id, title, bounds }]` по окнам терминала.
- Номер виртуального стола в конфиге и в состоянии хранится **1-based** (как `rule.desktop` в `placement.js:130`), а `virtualDesktop.GetWindowDesktopNumber()` возвращает **0-based**. Преобразование `+1` делается ровно в одном месте — при записи в слот.
- Ветка: `feat/claude-wt`. Коммиты — conventional commits, по одному на задачу.
- Работа идёт в `~/projects/js/windows11-manager`, кроме задачи 8 — она в `~/projects/shell/ccfzf`.

---

### Task 1: Индекс сессий из дампа ccfzf

Дамп `~/.ccfzf.sessions.json` (с Windows виден как `V:\.ccfzf.sessions.json`) содержит 200 новейших сессий по всем проектам. Нужен индекс `заголовок → сессия` с разрешением коллизий: одинаковые заголовки в дампе на 200 записей неизбежны.

**Files:**
- Create: `src/claude-wt/sessions-helpers.js`
- Create: `src/claude-wt/sessions-helpers.test.js`
- Create: `src/claude-wt/sessions.js`

**Interfaces:**
- Consumes: ничего.
- Produces:
  - `compareSessions(a, b) → number` — компаратор: сначала `live: true`, затем больший `mtime`.
  - `indexSessions(dump) → { [title]: { id, cwd, title, ambiguous } }`
  - `loadSessionIndex(filePath) → { [title]: {...} }` — читает файл, кэширует по mtime, при любой ошибке возвращает `{}`.

- [ ] **Step 1: Написать падающий тест**

Create `src/claude-wt/sessions-helpers.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { indexSessions, compareSessions } from './sessions-helpers.js';

const session = (over = {}) => ({
  id: 'a1', cwd: '/home/popstas/p', title: 'ccfzf', mtime: 100, live: false, ...over,
});

describe('indexSessions', () => {
  it('indexes sessions by title', () => {
    const index = indexSessions({ sessions: [session()] });
    expect(index.ccfzf).toEqual({ id: 'a1', cwd: '/home/popstas/p', title: 'ccfzf', ambiguous: false });
  });

  it('prefers a live session over a dead one with the same title', () => {
    const index = indexSessions({ sessions: [
      session({ id: 'dead', mtime: 500, live: false }),
      session({ id: 'live', mtime: 100, live: true }),
    ] });
    expect(index.ccfzf.id).toBe('live');
    expect(index.ccfzf.ambiguous).toBe(false);
  });

  it('prefers the newer session when liveness is equal', () => {
    const index = indexSessions({ sessions: [
      session({ id: 'old', mtime: 100 }),
      session({ id: 'new', mtime: 900 }),
    ] });
    expect(index.ccfzf.id).toBe('new');
  });

  it('marks a title ambiguous when the top two are indistinguishable', () => {
    const index = indexSessions({ sessions: [
      session({ id: 'x', mtime: 100, live: true }),
      session({ id: 'y', mtime: 100, live: true }),
    ] });
    expect(index.ccfzf.ambiguous).toBe(true);
  });

  it('skips entries without an id or a title', () => {
    const index = indexSessions({ sessions: [
      session({ id: '', title: 'no-id' }),
      session({ title: '', id: 'no-title' }),
      session({ id: 'ok', title: 'ok' }),
    ] });
    expect(Object.keys(index)).toEqual(['ok']);
  });

  it('returns an empty index for a missing or malformed dump', () => {
    expect(indexSessions(null)).toEqual({});
    expect(indexSessions({})).toEqual({});
    expect(indexSessions({ sessions: 'nope' })).toEqual({});
  });
});

describe('compareSessions', () => {
  it('returns 0 for sessions with equal liveness and mtime', () => {
    expect(compareSessions(session(), session({ id: 'b1' }))).toBe(0);
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx vitest run src/claude-wt/sessions-helpers.test.js`
Expected: FAIL — `Failed to resolve import "./sessions-helpers.js"`

- [ ] **Step 3: Реализовать хелперы**

Create `src/claude-wt/sessions-helpers.js`:

```js
/** Pure helper functions for the ccfzf session dump. No external I/O. */

/** Newest live session wins: live first, then larger mtime. */
function compareSessions(a, b) {
  const aLive = a.live ? 1 : 0;
  const bLive = b.live ? 1 : 0;
  if (aLive !== bLive) return bLive - aLive;
  return (b.mtime ?? 0) - (a.mtime ?? 0);
}

/**
 * Build a title -> session index out of a ccfzf dump.
 * A title shared by two equally good sessions is marked ambiguous:
 * the tracker refuses to move a window it cannot attribute.
 */
function indexSessions(dump) {
  const sessions = Array.isArray(dump?.sessions) ? dump.sessions : [];
  const byTitle = new Map();
  for (const s of sessions) {
    if (!s?.id || !s?.title) continue;
    if (!byTitle.has(s.title)) byTitle.set(s.title, []);
    byTitle.get(s.title).push(s);
  }
  const index = {};
  for (const [title, list] of byTitle) {
    const sorted = [...list].sort(compareSessions);
    const [best, second] = sorted;
    index[title] = {
      id: best.id,
      cwd: best.cwd ?? '',
      title,
      ambiguous: Boolean(second) && compareSessions(best, second) === 0,
    };
  }
  return index;
}

export { compareSessions, indexSessions };
```

- [ ] **Step 4: Убедиться, что тест проходит**

Run: `npx vitest run src/claude-wt/sessions-helpers.test.js`
Expected: PASS, 7 тестов

- [ ] **Step 5: Реализовать чтение файла**

Create `src/claude-wt/sessions.js`:

```js
import fs from 'node:fs';
import { indexSessions } from './sessions-helpers.js';

let cache = { path: '', mtimeMs: 0, index: {} };
let lastWarnedAt = 0;
const WARN_INTERVAL_MS = 5 * 60 * 1000;

/** The daemon ticks once a second; an unthrottled warning would be its own problem. */
function warnThrottled(message) {
  const now = Date.now();
  if (now - lastWarnedAt < WARN_INTERVAL_MS) return;
  lastWarnedAt = now;
  console.error(`[claude-wt] ${message}`);
}

/**
 * Read the ccfzf dump and index it. The file is re-read only when its mtime
 * changes: the daemon asks for the index once a second, the dump changes a few
 * times a day. Any failure (V: not mounted, truncated write) yields an empty
 * index — the tracker falls back to its own title history.
 */
function loadSessionIndex(filePath) {
  if (!filePath) return {};
  let stat;
  try {
    stat = fs.statSync(filePath);
  } catch (e) {
    warnThrottled(`session dump unreachable (${filePath}): ${e.message}`);
    return cache.path === filePath ? cache.index : {};
  }
  if (cache.path === filePath && cache.mtimeMs === stat.mtimeMs) return cache.index;
  try {
    const index = indexSessions(JSON.parse(fs.readFileSync(filePath, 'utf8')));
    cache = { path: filePath, mtimeMs: stat.mtimeMs, index };
  } catch (e) {
    warnThrottled(`session dump unreadable (${filePath}): ${e.message}`);
    cache = { path: filePath, mtimeMs: stat.mtimeMs, index: {} };
  }
  return cache.index;
}

export { indexSessions, compareSessions } from './sessions-helpers.js';
export { loadSessionIndex };
```

- [ ] **Step 6: Проверить линт и весь набор тестов**

Run: `npm run lint && npm test`
Expected: линт без ошибок, все тесты зелёные

- [ ] **Step 7: Коммит**

```bash
git add src/claude-wt/sessions-helpers.js src/claude-wt/sessions-helpers.test.js src/claude-wt/sessions.js
git commit -m "feat(claude-wt): индекс сессий из дампа ccfzf"
```

---

### Task 2: Файл состояния

Состояние ключуется по id сессии. Демон пишет его раз в секунду, поэтому запись атомарная (tmp + rename): оборванная запись в момент отключения питания уничтожила бы ровно те данные, ради которых всё затевалось.

**Files:**
- Create: `src/claude-wt/state-helpers.js`
- Create: `src/claude-wt/state-helpers.test.js`
- Create: `src/claude-wt/state.js`

**Interfaces:**
- Consumes: ничего.
- Produces:
  - `emptyState() → { version: 1, slots: {}, lastLayout: [], updated: 0 }`
  - `normalizeState(raw) → state` — чинит или отбрасывает мусор, всегда возвращает валидное состояние.
  - `rememberTitle(titles, title) → string[]` — новейший первым, без повторов, максимум 10.
  - `upsertSlot(slot, { title, cwd, bounds, desktop, now }) → slot` — обновляет только переданные поля.
  - `readState(filePath) → state` (из `state.js`), `writeState(filePath, state) → void` (из `state.js`).

- [ ] **Step 1: Написать падающий тест**

Create `src/claude-wt/state-helpers.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { emptyState, normalizeState, rememberTitle, upsertSlot } from './state-helpers.js';

describe('rememberTitle', () => {
  it('puts the newest title first', () => {
    expect(rememberTitle(['a', 'b'], 'c')).toEqual(['c', 'a', 'b']);
  });

  it('moves an already known title to the front instead of duplicating it', () => {
    expect(rememberTitle(['a', 'b'], 'b')).toEqual(['b', 'a']);
  });

  it('caps the history at 10 entries', () => {
    const many = Array.from({ length: 12 }, (_, i) => `t${i}`);
    expect(rememberTitle(many, 'new')).toHaveLength(10);
  });

  it('ignores an empty title', () => {
    expect(rememberTitle(['a'], '')).toEqual(['a']);
  });
});

describe('upsertSlot', () => {
  const bounds = { x: 1, y: 2, width: 3, height: 4 };

  it('creates a slot from scratch', () => {
    const slot = upsertSlot(undefined, { title: 'ccfzf', cwd: '/p', bounds, now: 1000 });
    expect(slot).toEqual({ titles: ['ccfzf'], cwd: '/p', bounds, desktop: null, lastSeen: 1000 });
  });

  it('updates bounds without touching the desktop number', () => {
    const before = upsertSlot(undefined, { title: 'ccfzf', bounds, desktop: 2, now: 1000 });
    const after = upsertSlot(before, { bounds: { x: 9, y: 9, width: 3, height: 4 }, now: 2000 });
    expect(after.desktop).toBe(2);
    expect(after.bounds.x).toBe(9);
    expect(after.lastSeen).toBe(2000);
  });

  it('appends a renamed session title to the history', () => {
    const before = upsertSlot(undefined, { title: 'ccfzf', bounds, now: 1000 });
    const after = upsertSlot(before, { title: 'ccfzf CLI', now: 2000 });
    expect(after.titles).toEqual(['ccfzf CLI', 'ccfzf']);
  });
});

describe('normalizeState', () => {
  it('returns an empty state for anything unusable', () => {
    expect(normalizeState(null)).toEqual(emptyState());
    expect(normalizeState({ version: 99 })).toEqual(emptyState());
    expect(normalizeState('nope')).toEqual(emptyState());
  });

  it('drops slots without bounds or titles but keeps the rest', () => {
    const state = normalizeState({
      version: 1,
      slots: {
        good: { titles: ['a'], bounds: { x: 0, y: 0, width: 10, height: 10 }, lastSeen: 5 },
        broken: { titles: [], bounds: null },
      },
      lastLayout: ['good', 42],
      updated: 7,
    });
    expect(Object.keys(state.slots)).toEqual(['good']);
    expect(state.lastLayout).toEqual(['good']);
    expect(state.updated).toBe(7);
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx vitest run src/claude-wt/state-helpers.test.js`
Expected: FAIL — `Failed to resolve import "./state-helpers.js"`

- [ ] **Step 3: Реализовать хелперы**

Create `src/claude-wt/state-helpers.js`:

```js
/** Pure helper functions for claude-wt state. No external I/O. */

const STATE_VERSION = 1;
const MAX_TITLES = 10;

function emptyState() {
  return { version: STATE_VERSION, slots: {}, lastLayout: [], updated: 0 };
}

/** Newest first, no duplicates, bounded — a session renamed all day must not grow the file. */
function rememberTitle(titles, title) {
  const list = Array.isArray(titles) ? titles : [];
  if (!title) return list;
  return [title, ...list.filter(t => t !== title)].slice(0, MAX_TITLES);
}

function isBounds(b) {
  return Boolean(b) && ['x', 'y', 'width', 'height'].every(k => Number.isFinite(b[k]));
}

/** Update only the fields that were passed; everything else is carried over. */
function upsertSlot(slot, { title, cwd, bounds, desktop, now } = {}) {
  const base = slot ?? { titles: [], cwd: '', bounds: null, desktop: null, lastSeen: 0 };
  return {
    titles: title ? rememberTitle(base.titles, title) : base.titles,
    cwd: cwd ?? base.cwd,
    bounds: bounds ?? base.bounds,
    desktop: desktop ?? base.desktop,
    lastSeen: now ?? base.lastSeen,
  };
}

function normalizeState(raw) {
  if (!raw || typeof raw !== 'object' || raw.version !== STATE_VERSION) return emptyState();
  const slots = {};
  for (const [id, slot] of Object.entries(raw.slots ?? {})) {
    if (!slot || !Array.isArray(slot.titles) || !slot.titles.length) continue;
    if (!isBounds(slot.bounds)) continue;
    slots[id] = {
      titles: slot.titles.filter(t => typeof t === 'string' && t).slice(0, MAX_TITLES),
      cwd: typeof slot.cwd === 'string' ? slot.cwd : '',
      bounds: slot.bounds,
      desktop: Number.isFinite(slot.desktop) ? slot.desktop : null,
      lastSeen: Number.isFinite(slot.lastSeen) ? slot.lastSeen : 0,
    };
  }
  const lastLayout = (Array.isArray(raw.lastLayout) ? raw.lastLayout : [])
    .filter(id => typeof id === 'string' && slots[id]);
  return {
    version: STATE_VERSION,
    slots,
    lastLayout,
    updated: Number.isFinite(raw.updated) ? raw.updated : 0,
  };
}

export { STATE_VERSION, emptyState, rememberTitle, upsertSlot, normalizeState, isBounds };
```

- [ ] **Step 4: Убедиться, что тест проходит**

Run: `npx vitest run src/claude-wt/state-helpers.test.js`
Expected: PASS, 9 тестов

- [ ] **Step 5: Реализовать чтение и запись**

Create `src/claude-wt/state.js`:

```js
import fs from 'node:fs';
import path from 'node:path';
import { emptyState, normalizeState } from './state-helpers.js';

/**
 * Read the state file. A file broken by a power loss is moved aside as .bak
 * rather than deleted: the daemon must start, but the bytes may still be
 * worth looking at.
 */
function readState(filePath) {
  if (!filePath || !fs.existsSync(filePath)) return emptyState();
  try {
    return normalizeState(JSON.parse(fs.readFileSync(filePath, 'utf8')));
  } catch (e) {
    console.error(`[claude-wt] broken state file, moving to .bak: ${e.message}`);
    try { fs.renameSync(filePath, `${filePath}.bak`); } catch { /* nothing else to do */ }
    return emptyState();
  }
}

/** Atomic: write a sibling temp file, then rename over the target. */
function writeState(filePath, state) {
  if (!filePath) return;
  fs.mkdirSync(path.dirname(filePath), { recursive: true });
  const tmp = `${filePath}.tmp`;
  fs.writeFileSync(tmp, JSON.stringify(state));
  fs.renameSync(tmp, filePath);
}

export * from './state-helpers.js';
export { readState, writeState };
```

- [ ] **Step 6: Проверить линт и весь набор тестов**

Run: `npm run lint && npm test`
Expected: линт без ошибок, все тесты зелёные

- [ ] **Step 7: Коммит**

```bash
git add src/claude-wt/state-helpers.js src/claude-wt/state-helpers.test.js src/claude-wt/state.js
git commit -m "feat(claude-wt): файл состояния с атомарной записью"
```

---

### Task 3: Стабильность заголовка и разрешение окна в сессию

Заголовок при входе в сессию меняется несколько раз подряд (шелл → claude → название сессии). Реагировать нужно только на устоявшийся. Разрешение заголовка в сессию идёт по ступеням: дамп ccfzf, затем собственная история заголовков в слотах.

**Files:**
- Create: `src/claude-wt/tracker-helpers.js`
- Create: `src/claude-wt/tracker-helpers.test.js`

**Interfaces:**
- Consumes: `indexSessions` из Task 1 (формат индекса), `upsertSlot` из Task 2.
- Produces:
  - `trackTitle(before, win, stableTicks) → { id, title, titleTicks, stableTitle, sessionId, bounds, pendingMove }`
  - `duplicateTitles(windows) → Set<string>` — заголовки, встречающиеся в текущем снимке больше одного раза.
  - `resolveSession(title, sessionIndex, slots) → { id, cwd, ambiguous } | null`

- [ ] **Step 1: Написать падающий тест**

Create `src/claude-wt/tracker-helpers.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { trackTitle, duplicateTitles, resolveSession } from './tracker-helpers.js';

const win = (over = {}) => ({ id: 1, title: 'ccfzf', bounds: { x: 0, y: 0, width: 800, height: 600 }, ...over });

describe('trackTitle', () => {
  it('does not make a brand new title stable before stableTicks', () => {
    const t = trackTitle(undefined, win(), 2);
    expect(t.titleTicks).toBe(1);
    expect(t.stableTitle).toBe(null);
  });

  it('makes a title stable once it has held for stableTicks', () => {
    const first = trackTitle(undefined, win(), 2);
    const second = trackTitle(first, win(), 2);
    expect(second.stableTitle).toBe('ccfzf');
  });

  it('restarts the count when the title changes', () => {
    const first = trackTitle(undefined, win(), 2);
    const second = trackTitle(first, win(), 2);
    const third = trackTitle(second, win({ title: 'other' }), 2);
    expect(third.titleTicks).toBe(1);
    expect(third.stableTitle).toBe('ccfzf');   // прежний стабильный держится, пока новый не устоялся
  });

  it('carries over sessionId and pendingMove', () => {
    const before = { ...trackTitle(undefined, win(), 1), sessionId: 'a1', pendingMove: { since: 5 } };
    const after = trackTitle(before, win(), 1);
    expect(after.sessionId).toBe('a1');
    expect(after.pendingMove).toEqual({ since: 5 });
  });
});

describe('duplicateTitles', () => {
  it('reports a title shown by two windows at once', () => {
    const dup = duplicateTitles([win({ id: 1 }), win({ id: 2 }), win({ id: 3, title: 'other' })]);
    expect([...dup]).toEqual(['ccfzf']);
  });

  it('reports nothing when every title is unique', () => {
    expect(duplicateTitles([win({ id: 1 }), win({ id: 2, title: 'other' })]).size).toBe(0);
  });
});

describe('resolveSession', () => {
  const index = { ccfzf: { id: 'a1', cwd: '/p', title: 'ccfzf', ambiguous: false } };

  it('resolves from the ccfzf index', () => {
    expect(resolveSession('ccfzf', index, {})).toEqual({ id: 'a1', cwd: '/p', ambiguous: false });
  });

  it('falls back to the title history in state when the dump is unavailable', () => {
    const slots = { a1: { titles: ['ccfzf'], cwd: '/p', bounds: {}, desktop: null, lastSeen: 0 } };
    expect(resolveSession('ccfzf', {}, slots)).toEqual({ id: 'a1', cwd: '/p', ambiguous: false });
  });

  it('marks the fallback ambiguous when two slots claim the same title', () => {
    const slots = {
      a1: { titles: ['ccfzf'], cwd: '/p', bounds: {}, desktop: null, lastSeen: 10 },
      b2: { titles: ['ccfzf'], cwd: '/q', bounds: {}, desktop: null, lastSeen: 20 },
    };
    expect(resolveSession('ccfzf', {}, slots).ambiguous).toBe(true);
  });

  it('returns null for a shell prompt title', () => {
    expect(resolveSession('popstas@pc-virt: ~/projects', index, {})).toBe(null);
    expect(resolveSession(null, index, {})).toBe(null);
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx vitest run src/claude-wt/tracker-helpers.test.js`
Expected: FAIL — `Failed to resolve import "./tracker-helpers.js"`

- [ ] **Step 3: Реализовать хелперы**

Create `src/claude-wt/tracker-helpers.js`:

```js
/** Pure helper functions for the claude-wt tracker. No external I/O. */

/**
 * Follow one window's title across ticks. A title becomes "stable" only after
 * it has held for stableTicks: entering a session flips the title two or three
 * times in a row (shell -> claude -> session name), and reacting to the
 * intermediate values would bind the window to the wrong thing.
 */
function trackTitle(before, win, stableTicks) {
  const sameTitle = before?.title === win.title;
  const titleTicks = sameTitle ? before.titleTicks + 1 : 1;
  const settled = titleTicks >= stableTicks;
  return {
    id: win.id,
    title: win.title,
    titleTicks,
    stableTitle: settled ? win.title : (before?.stableTitle ?? null),
    sessionId: before?.sessionId ?? null,
    bounds: win.bounds,
    pendingMove: before?.pendingMove ?? null,
  };
}

/** Titles shown by more than one window right now — those windows stay put. */
function duplicateTitles(windows) {
  const seen = new Map();
  for (const w of windows) {
    if (!w.title) continue;
    seen.set(w.title, (seen.get(w.title) ?? 0) + 1);
  }
  return new Set([...seen].filter(([, n]) => n > 1).map(([title]) => title));
}

/**
 * Title -> session, first from the ccfzf dump, then from our own title history.
 * The fallback keeps the module working while V: is unmounted or the dump is
 * stale, at the cost of only knowing sessions we have already seen.
 */
function resolveSession(title, sessionIndex, slots) {
  if (!title) return null;
  const fromDump = sessionIndex?.[title];
  if (fromDump) return { id: fromDump.id, cwd: fromDump.cwd, ambiguous: fromDump.ambiguous };
  const matches = Object.entries(slots ?? {}).filter(([, slot]) => slot.titles?.includes(title));
  if (!matches.length) return null;
  const [id, slot] = matches[0];
  return { id, cwd: slot.cwd ?? '', ambiguous: matches.length > 1 };
}

export { trackTitle, duplicateTitles, resolveSession };
```

- [ ] **Step 4: Убедиться, что тест проходит**

Run: `npx vitest run src/claude-wt/tracker-helpers.test.js`
Expected: PASS, 10 тестов

- [ ] **Step 5: Коммит**

```bash
git add src/claude-wt/tracker-helpers.js src/claude-wt/tracker-helpers.test.js
git commit -m "feat(claude-wt): стабильность заголовка и разрешение окна в сессию"
```

---

### Task 4: Ядро трекера — step()

Сердце модуля: из предыдущего и текущего снимка окон выводит новое состояние, список перемещений и список окон, которым нужно один раз прочитать номер виртуального стола.

**Files:**
- Modify: `src/claude-wt/tracker-helpers.js` (дописать `step` и внутренние функции)
- Modify: `src/claude-wt/tracker-helpers.test.js` (дописать `describe('step')`)

**Interfaces:**
- Consumes: `trackTitle`, `duplicateTitles`, `resolveSession` из Task 3; `upsertSlot`, `isBounds` из Task 2 (`./state-helpers.js`).
- Produces:
  - `step({ prevWindows, windows, sessionIndex, state, now, options }) → { nextWindows, actions, bindings, nextState }`
    - `windows`: `[{ id, title, bounds: { x, y, width, height } }]`
    - `actions`: `[{ windowId, bounds, desktop }]` — что подвинуть
    - `bindings`: `[{ windowId, sessionId }]` — кому прочитать номер стола
    - `options`: `{ stableTicks = 2, moveTimeoutMs = 5000, minimizedX = -10000 }`

- [ ] **Step 1: Написать падающий тест**

Дописать в конец `src/claude-wt/tracker-helpers.test.js`:

```js
import { step } from './tracker-helpers.js';
import { emptyState, upsertSlot } from './state-helpers.js';

const bounds = (x, y) => ({ x, y, width: 800, height: 600 });
const index = { ccfzf: { id: 'a1', cwd: '/p', title: 'ccfzf', ambiguous: false } };

// Прогоняет тики, пока заголовок не станет стабильным, и отдаёт последний результат.
function run(ticks, { state = emptyState(), sessionIndex = index } = {}) {
  let prevWindows = [];
  let out = { nextWindows: [], actions: [], bindings: [], nextState: state };
  ticks.forEach((windows, i) => {
    out = step({ prevWindows, windows, sessionIndex, state: out.nextState, now: 1000 + i * 1000 });
    prevWindows = out.nextWindows;
  });
  return out;
}

describe('step', () => {
  it('creates a slot with the current position for a session it has never seen', () => {
    const w = [{ id: 1, title: 'ccfzf', bounds: bounds(10, 20) }];
    const out = run([w, w]);
    expect(out.actions).toEqual([]);
    expect(out.nextState.slots.a1.bounds).toEqual(bounds(10, 20));
    expect(out.bindings).toEqual([{ windowId: 1, sessionId: 'a1' }]);
  });

  it('moves the window back to the remembered position when the session returns', () => {
    const state = { ...emptyState(), slots: { a1: upsertSlot(undefined, { title: 'ccfzf', bounds: bounds(500, 500), desktop: 2, now: 1 }) } };
    const shell = [{ id: 1, title: 'popstas@pc-virt: ~', bounds: bounds(0, 0) }];
    const session = [{ id: 1, title: 'ccfzf', bounds: bounds(0, 0) }];
    const out = run([shell, shell, session, session], { state });
    expect(out.actions).toEqual([{ windowId: 1, bounds: bounds(500, 500), desktop: 2 }]);
  });

  it('records a position the user dragged the window to', () => {
    const w = [{ id: 1, title: 'ccfzf', bounds: bounds(10, 20) }];
    const moved = [{ id: 1, title: 'ccfzf', bounds: bounds(700, 300) }];
    const out = run([w, w, moved]);
    expect(out.nextState.slots.a1.bounds).toEqual(bounds(700, 300));
  });

  it('does not record the position while its own move is still in flight', () => {
    const state = { ...emptyState(), slots: { a1: upsertSlot(undefined, { title: 'ccfzf', bounds: bounds(500, 500), now: 1 }) } };
    const shell = [{ id: 1, title: 'x@y: ~', bounds: bounds(0, 0) }];
    const session = [{ id: 1, title: 'ccfzf', bounds: bounds(0, 0) }];
    const out = run([shell, shell, session, session], { state });
    expect(out.nextState.slots.a1.bounds).toEqual(bounds(500, 500));
  });

  it('leaves the window alone when the title is not a session', () => {
    const w = [{ id: 1, title: 'popstas@pc-virt: ~/projects', bounds: bounds(10, 20) }];
    const out = run([w, w]);
    expect(out.actions).toEqual([]);
    expect(out.nextState.slots).toEqual({});
  });

  it('does not touch either window when two of them show the same title', () => {
    const w = [
      { id: 1, title: 'ccfzf', bounds: bounds(10, 20) },
      { id: 2, title: 'ccfzf', bounds: bounds(30, 40) },
    ];
    const out = run([w, w]);
    expect(out.actions).toEqual([]);
    expect(out.nextState.slots).toEqual({});
  });

  it('refuses to bind an ambiguous title', () => {
    const w = [{ id: 1, title: 'ccfzf', bounds: bounds(10, 20) }];
    const ambiguous = { ccfzf: { id: 'a1', cwd: '/p', title: 'ccfzf', ambiguous: true } };
    const out = run([w, w], { sessionIndex: ambiguous });
    expect(out.nextState.slots).toEqual({});
  });

  it('never records a minimized window', () => {
    const w = [{ id: 1, title: 'ccfzf', bounds: { x: -32000, y: -32000, width: 800, height: 600 } }];
    const out = run([w, w]);
    expect(out.nextState.slots).toEqual({});
  });

  it('keeps the slot but clears the layout when the window disappears', () => {
    const w = [{ id: 1, title: 'ccfzf', bounds: bounds(10, 20) }];
    const out = run([w, w, []]);
    expect(out.nextState.slots.a1.bounds).toEqual(bounds(10, 20));
    expect(out.nextState.lastLayout).toEqual([]);
  });

  it('lists the sessions on screen in lastLayout', () => {
    const two = {
      ccfzf: { id: 'a1', cwd: '/p', title: 'ccfzf', ambiguous: false },
      home: { id: 'b2', cwd: '/q', title: 'home', ambiguous: false },
    };
    const w = [
      { id: 1, title: 'ccfzf', bounds: bounds(10, 20) },
      { id: 2, title: 'home', bounds: bounds(30, 40) },
    ];
    const out = run([w, w], { sessionIndex: two });
    expect(out.nextState.lastLayout.sort()).toEqual(['a1', 'b2']);
  });

  it('stamps updated with the current time in seconds', () => {
    const w = [{ id: 1, title: 'ccfzf', bounds: bounds(10, 20) }];
    const out = run([w, w]);
    expect(out.nextState.updated).toBe(2);
  });
});
```

- [ ] **Step 2: Убедиться, что новые тесты падают**

Run: `npx vitest run src/claude-wt/tracker-helpers.test.js`
Expected: FAIL — `step is not a function` (или ошибка импорта `step`)

- [ ] **Step 3: Реализовать step()**

Добавить импорт **первой строкой** файла `src/claude-wt/tracker-helpers.js`, под комментарием-шапкой:

```js
import { upsertSlot } from './state-helpers.js';
```

Затем дописать перед строкой `export`:

```js
const DEFAULTS = { stableTicks: 2, moveTimeoutMs: 5000, minimizedX: -10000 };

function boundsEqual(a, b) {
  if (!a || !b) return false;
  return ['x', 'y', 'width', 'height'].every(k => a[k] === b[k]);
}

/**
 * One tick of the tracker: pure, so the whole behaviour table is testable
 * without Windows. Returns the next state plus what the daemon must do:
 * `actions` are windows to move, `bindings` are windows whose virtual desktop
 * number should be read once (that call spawns VirtualDesktop11.exe, so it
 * must never happen on every tick).
 */
function step({ prevWindows = [], windows = [], sessionIndex = {}, state, now, options = {} }) {
  const { stableTicks, moveTimeoutMs, minimizedX } = { ...DEFAULTS, ...options };
  const prev = new Map(prevWindows.map(w => [w.id, w]));
  const duplicates = duplicateTitles(windows);
  const slots = { ...state.slots };
  const nextWindows = [];
  const actions = [];
  const bindings = [];
  const lastLayout = [];

  for (const win of windows) {
    const before = prev.get(win.id);
    const tracked = trackTitle(before, win, stableTicks);
    const minimized = win.bounds.x < minimizedX;
    const titleChanged = tracked.stableTitle !== (before?.stableTitle ?? null);

    if (titleChanged) {
      const resolved = duplicates.has(tracked.stableTitle)
        ? null
        : resolveSession(tracked.stableTitle, sessionIndex, slots);
      tracked.sessionId = resolved && !resolved.ambiguous ? resolved.id : null;
      if (tracked.sessionId) {
        const known = slots[tracked.sessionId];
        const common = { title: tracked.stableTitle, cwd: resolved.cwd, now };
        if (known?.bounds) {
          slots[tracked.sessionId] = upsertSlot(known, common);
          actions.push({ windowId: win.id, bounds: known.bounds, desktop: known.desktop });
          tracked.pendingMove = { bounds: known.bounds, since: now };
        } else if (!minimized) {
          // Слот заводится только вместе с позицией: слот без bounds бесполезен
          // и был бы всё равно выброшен normalizeState при следующем чтении.
          slots[tracked.sessionId] = upsertSlot(known, { ...common, bounds: win.bounds });
          bindings.push({ windowId: win.id, sessionId: tracked.sessionId });
        }
      }
    } else if (tracked.sessionId) {
      const settled = !tracked.pendingMove
        || boundsEqual(win.bounds, tracked.pendingMove.bounds)
        || now - tracked.pendingMove.since > moveTimeoutMs;
      if (settled) {
        tracked.pendingMove = null;
        if (!minimized) {
          slots[tracked.sessionId] = upsertSlot(slots[tracked.sessionId], { bounds: win.bounds, now });
        }
      }
    }

    if (tracked.sessionId) lastLayout.push(tracked.sessionId);
    nextWindows.push(tracked);
  }

  return {
    nextWindows,
    actions,
    bindings,
    nextState: { ...state, slots, lastLayout, updated: Math.floor(now / 1000) },
  };
}
```

И заменить строку экспорта на:

```js
export { trackTitle, duplicateTitles, resolveSession, step, boundsEqual };
```

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `npx vitest run src/claude-wt/tracker-helpers.test.js`
Expected: PASS, 21 тест

- [ ] **Step 5: Проверить линт и весь набор тестов**

Run: `npm run lint && npm test`
Expected: линт без ошибок, все тесты зелёные

- [ ] **Step 6: Коммит**

```bash
git add src/claude-wt/tracker-helpers.js src/claude-wt/tracker-helpers.test.js
git commit -m "feat(claude-wt): ядро трекера step()"
```

---

### Task 5: Зажать позицию в границы существующих мониторов

Сохранённая позиция может указывать на монитор, которого больше нет (отключил второй экран). Окно окажется за пределами видимой области и станет недоступным.

**Files:**
- Modify: `src/geometry.js` (дописать функцию и добавить в экспорт)
- Modify: `src/geometry.test.js` (дописать `describe`)

**Interfaces:**
- Consumes: ничего.
- Produces: `clampBoundsToMonitors(bounds, monitors) → { x, y, width, height }`, где `monitors` — массив `{ bounds: { x, y, width, height } }` (форма из `getWindowsMonitors()`).

- [ ] **Step 1: Написать падающий тест**

Дописать в конец `src/geometry.test.js`:

```js
import { clampBoundsToMonitors } from './geometry.js';

describe('clampBoundsToMonitors', () => {
  const mons = [{ bounds: { x: 0, y: 0, width: 1920, height: 1080 } }];

  it('leaves bounds that fit on a monitor untouched', () => {
    const b = { x: 100, y: 100, width: 800, height: 600 };
    expect(clampBoundsToMonitors(b, mons)).toEqual(b);
  });

  it('pulls a window back from a monitor that is gone', () => {
    const b = { x: 3000, y: 200, width: 800, height: 600 };
    expect(clampBoundsToMonitors(b, mons)).toEqual({ x: 1120, y: 200, width: 800, height: 600 });
  });

  it('pulls a window back from negative coordinates', () => {
    const b = { x: -2000, y: -500, width: 800, height: 600 };
    expect(clampBoundsToMonitors(b, mons)).toEqual({ x: 0, y: 0, width: 800, height: 600 });
  });

  it('shrinks a window wider than every monitor', () => {
    const b = { x: 0, y: 0, width: 4000, height: 600 };
    expect(clampBoundsToMonitors(b, mons)).toEqual({ x: 0, y: 0, width: 1920, height: 600 });
  });

  it('keeps bounds as-is when the monitor list is empty', () => {
    const b = { x: 3000, y: 200, width: 800, height: 600 };
    expect(clampBoundsToMonitors(b, [])).toEqual(b);
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx vitest run src/geometry.test.js`
Expected: FAIL — `clampBoundsToMonitors is not a function`

- [ ] **Step 3: Реализовать функцию**

Дописать в `src/geometry.js` перед строкой `export`:

```js
function boundsOverlap(a, b) {
  const w = Math.min(a.x + a.width, b.x + b.width) - Math.max(a.x, b.x);
  const h = Math.min(a.y + a.height, b.y + b.height) - Math.max(a.y, b.y);
  return w > 0 && h > 0 ? w * h : 0;
}

/**
 * Keep a remembered position reachable. If the monitor it was saved on is
 * gone, pull the window onto the monitor it overlaps most (the first one when
 * it overlaps nothing) instead of leaving it off-screen.
 */
function clampBoundsToMonitors(bounds, monitors) {
  if (!monitors?.length) return bounds;
  const best = monitors.reduce((acc, mon) =>
    boundsOverlap(bounds, mon.bounds) > boundsOverlap(bounds, acc.bounds) ? mon : acc, monitors[0]);
  const area = best.bounds;
  const width = Math.min(bounds.width, area.width);
  const height = Math.min(bounds.height, area.height);
  return {
    x: Math.max(area.x, Math.min(bounds.x, area.x + area.width - width)),
    y: Math.max(area.y, Math.min(bounds.y, area.y + area.height - height)),
    width,
    height,
  };
}
```

И добавить `clampBoundsToMonitors` в список в строке `export {...}`.

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `npx vitest run src/geometry.test.js`
Expected: PASS, включая 5 новых тестов

- [ ] **Step 5: Коммит**

```bash
git add src/geometry.js src/geometry.test.js
git commit -m "feat(geometry): зажимать bounds в границы существующих мониторов"
```

---

### Task 6: Демон и команда CLI `claude-wt watch`

Связывает всё вместе: таймер, снимок окон терминала, вызов `step`, исполнение перемещений через существующий `placeWindowByConfig`, разовое чтение номера виртуального стола, запись состояния.

**Files:**
- Create: `src/claude-wt/index.js`
- Modify: `src/lib/index.js` (добавить реэкспорт)
- Modify: `src/index.js` (добавить команду `claude-wt`)
- Modify: `config.example.cjs` (добавить блок `claudeWt`)

**Interfaces:**
- Consumes: `loadSessionIndex` (Task 1), `readState`/`writeState` (Task 2), `step` (Task 4), `clampBoundsToMonitors` (Task 5), `getWindows` из `../windows.js`, `placeWindowByConfig` из `../placement.js`, `getWindowsMonitors` из `../monitors.js`, `virtualDesktop` из `../virtual-desktop.js`, `getConfig` из `../config.js`.
- Produces:
  - `getClaudeWtConfig() → { enabled, interval, stableTicks, sessionsFile, statePath, desktop, launch, restore }` — конфиг с подставленными значениями по умолчанию.
  - `isTerminalWindow(w) → boolean`
  - `startClaudeWt() → void`, `stopClaudeWt() → void`
  - `claudeWtStatus() → { running, slots, lastLayout, statePath, sessionsFile }`

- [ ] **Step 1: Реализовать демон**

Create `src/claude-wt/index.js`:

```js
import { getConfig } from '../config.js';
import { getWindows } from '../windows.js';
import { placeWindowByConfig } from '../placement.js';
import { getWindowsMonitors } from '../monitors.js';
import { virtualDesktop } from '../virtual-desktop.js';
import { clampBoundsToMonitors } from '../geometry.js';
import { loadSessionIndex } from './sessions.js';
import { readState, writeState, upsertSlot } from './state.js';
import { step } from './tracker-helpers.js';

const DEFAULTS = {
  enabled: true,
  interval: 1000,
  stableTicks: 2,
  sessionsFile: '',
  statePath: '',
  desktop: true,
  launch: { command: 'wt.exe', args: [] },
  restore: { auto: false, windowTimeoutMs: 30000 },
};

function getClaudeWtConfig() {
  const cfg = getConfig().claudeWt ?? {};
  return {
    ...DEFAULTS,
    ...cfg,
    launch: { ...DEFAULTS.launch, ...(cfg.launch ?? {}) },
    restore: { ...DEFAULTS.restore, ...(cfg.restore ?? {}) },
  };
}

function isTerminalWindow(w) {
  return /WindowsTerminal\.exe$/i.test(w.path ?? '');
}

function snapshot() {
  const wins = getWindows().filter(isTerminalWindow);
  return wins.map(w => ({ id: w.id, title: w.title, bounds: w.getBounds() }));
}

let intervalId = null;
let prevWindows = [];
let lastWritten = '';
let liveState = null;

async function tick() {
  const cfg = getClaudeWtConfig();
  // Состояние живёт в памяти: с диска оно читается один раз при старте, дальше
  // файл — только снимок для восстановления, а не рабочая структура.
  if (!liveState) liveState = readState(cfg.statePath);
  const windows = snapshot();
  const sessionIndex = loadSessionIndex(cfg.sessionsFile);
  // monitors нужны ДО actions: step() зажимает координаты сам, иначе запрошенная
  // и фактически применённая позиция расходятся и guard собственного хода
  // висит до таймаута, а потом записывает зажатую позицию поверх исходной.
  const monitors = getWindowsMonitors();
  const { nextWindows, actions, bindings, nextState } = step({
    prevWindows,
    windows,
    sessionIndex,
    state: liveState,
    monitors,
    now: Date.now(),
    options: { stableTicks: cfg.stableTicks },
  });
  prevWindows = nextWindows;

  for (const action of actions) {
    // action.bounds уже зажаты внутри step() — повторно клампить не нужно
    const rule = { window: action.windowId, ...action.bounds };
    if (cfg.desktop && action.desktop) rule.desktop = action.desktop;
    try {
      await placeWindowByConfig(rule);
    } catch (e) {
      console.error(`[claude-wt] failed to place window ${action.windowId}: ${e.message}`);
    }
  }

  // Читаем номер виртуального стола только в момент привязки окна к сессии:
  // этот вызов спавнит VirtualDesktop11.exe, в горячем цикле ему не место.
  if (cfg.desktop) {
    for (const binding of bindings) {
      try {
        const num = await virtualDesktop.GetWindowDesktopNumber(binding.windowId);
        if (num !== undefined && nextState.slots[binding.sessionId]) {
          nextState.slots[binding.sessionId] = upsertSlot(nextState.slots[binding.sessionId], {
            desktop: Number(num) + 1,
          });
        }
      } catch (e) {
        console.error(`[claude-wt] failed to read desktop for ${binding.windowId}: ${e.message}`);
      }
    }
  }

  liveState = nextState;
  const serialized = JSON.stringify(nextState);
  if (serialized !== lastWritten) {
    writeState(cfg.statePath, nextState);
    lastWritten = serialized;
  }
}

function startClaudeWt() {
  const cfg = getClaudeWtConfig();
  if (!cfg.statePath) {
    console.error('[claude-wt] claudeWt.statePath is not set in config, refusing to start');
    return;
  }
  stopClaudeWt();
  liveState = null;
  prevWindows = [];
  console.log(`[claude-wt] watching every ${cfg.interval}ms, state: ${cfg.statePath}`);
  intervalId = setInterval(() => {
    tick().catch(e => console.error(`[claude-wt] tick failed: ${e.message}`));
  }, cfg.interval);
}

function stopClaudeWt() {
  if (intervalId !== null) {
    clearInterval(intervalId);
    intervalId = null;
  }
}

function claudeWtStatus() {
  const cfg = getClaudeWtConfig();
  const state = readState(cfg.statePath);
  return {
    running: intervalId !== null,
    slots: Object.entries(state.slots).map(([id, slot]) => ({
      id, title: slot.titles[0], bounds: slot.bounds, desktop: slot.desktop, lastSeen: slot.lastSeen,
    })),
    lastLayout: state.lastLayout,
    statePath: cfg.statePath,
    sessionsFile: cfg.sessionsFile,
  };
}

export { getClaudeWtConfig, isTerminalWindow, startClaudeWt, stopClaudeWt, claudeWtStatus, tick };
```

- [ ] **Step 2: Подключить к CLI**

В `src/index.js` дописать перед `program.allowExcessArguments();`:

```js
  const claudeWt = program.command('claude-wt').description('remember Claude Code window positions');

  claudeWt.command('watch').action(async () => {
    const mod = await import('./claude-wt/index.js');
    mod.startClaudeWt();
  });

  claudeWt.command('status').action(async () => {
    const mod = await import('./claude-wt/index.js');
    console.log(JSON.stringify(mod.claudeWtStatus(), null, 2));
    process.exit(0);
  });

  claudeWt.command('clear').action(async () => {
    const fs = await import('node:fs');
    const mod = await import('./claude-wt/index.js');
    const { statePath } = mod.getClaudeWtConfig();
    if (statePath && fs.existsSync(statePath)) fs.unlinkSync(statePath);
    console.log(`[claude-wt] cleared ${statePath}`);
    process.exit(0);
  });
```

В `src/lib/index.js` дописать строку:

```js
export * from '../claude-wt/index.js';
```

- [ ] **Step 3: Добавить блок в пример конфига**

В `config.example.cjs` дописать в объект конфига:

```js
  claudeWt: {
    enabled: true,
    interval: 1000,
    stableTicks: 2,
    sessionsFile: 'V:\\.ccfzf.sessions.json',
    statePath: 'C:\\Users\\popstas\\AppData\\Local\\windows11-manager\\claude-wt.json',
    desktop: true,
    launch: {
      command: 'wt.exe',
      args: ['-w', '-1', '-p', 'popstas', 'ssh', '-A', 'popstas@pc-virt.popstas.pro',
             '-t', 'ccfzf --session {id} --kiosk'],
    },
    restore: { auto: false, windowTimeoutMs: 30000 },
  },
```

- [ ] **Step 4: Проверить, что ничего не сломалось**

Run: `npm run lint && npm test`
Expected: линт без ошибок, все прежние тесты зелёные

- [ ] **Step 5: Проверить вручную на Windows**

Скопировать блок `claudeWt` в `~/.config/windows11-manager.config.js`, затем:

```
node src claude-wt status
```

Ожидается JSON с пустым `slots` и путями из конфига. После этого:

```
node src claude-wt watch
```

Открыть окно Windows Terminal, войти в сессию через `ccfzf`, подвинуть окно, выйти из claude (заголовок станет приглашением шелла), войти в ту же сессию снова. Ожидается: примерно через две секунды окно возвращается на прежнее место. В другом терминале `node src claude-wt status` показывает слот с координатами.

- [ ] **Step 6: Коммит**

```bash
git add src/claude-wt/index.js src/lib/index.js src/index.js config.example.cjs
git commit -m "feat(claude-wt): демон позиционной памяти и команда CLI"
```

---

### Task 7: Детект аварии и план восстановления

Отличить аварийную перезагрузку от намеренного закрытия по признаку «окон нет» нельзя. Точный признак — состояние записано до текущей загрузки системы при непустом `lastLayout`: при штатном закрытии демон жив и успевает опустошить layout.

**Files:**
- Create: `src/claude-wt/restore-helpers.js`
- Create: `src/claude-wt/restore-helpers.test.js`

**Interfaces:**
- Consumes: форму состояния из Task 2.
- Produces:
  - `bootTimeSec(uptimeSec, nowMs) → number`
  - `detectCrash({ state, bootTimeSec, windowCount }) → boolean`
  - `planRestore({ state, launch }) → [{ sessionId, title, command, args, bounds, desktop }]`

- [ ] **Step 1: Написать падающий тест**

Create `src/claude-wt/restore-helpers.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { bootTimeSec, detectCrash, planRestore } from './restore-helpers.js';

const bounds = { x: 10, y: 20, width: 800, height: 600 };
const slot = (over = {}) => ({ titles: ['ccfzf'], cwd: '/p', bounds, desktop: 2, lastSeen: 500, ...over });
const state = (over = {}) => ({
  version: 1, slots: { a1: slot() }, lastLayout: ['a1'], updated: 500, ...over,
});

describe('bootTimeSec', () => {
  it('subtracts uptime from now', () => {
    expect(bootTimeSec(600, 1_000_000_000)).toBe(999_400);
  });
});

describe('detectCrash', () => {
  it('reports a crash when the state predates the current boot', () => {
    expect(detectCrash({ state: state(), bootTimeSec: 1000, windowCount: 0 })).toBe(true);
  });

  it('reports nothing when the state was written after boot', () => {
    expect(detectCrash({ state: state({ updated: 2000 }), bootTimeSec: 1000, windowCount: 0 })).toBe(false);
  });

  it('reports nothing when the layout is empty — the user closed everything', () => {
    expect(detectCrash({ state: state({ lastLayout: [] }), bootTimeSec: 1000, windowCount: 0 })).toBe(false);
  });

  it('reports nothing when terminals are already open', () => {
    expect(detectCrash({ state: state(), bootTimeSec: 1000, windowCount: 2 })).toBe(false);
  });

  it('reports nothing for a state that was never written', () => {
    expect(detectCrash({ state: state({ updated: 0 }), bootTimeSec: 1000, windowCount: 0 })).toBe(false);
  });
});

describe('planRestore', () => {
  const launch = { command: 'wt.exe', args: ['-w', '-1', '-t', 'ccfzf --session {id} --kiosk'] };

  it('turns the last layout into launch commands', () => {
    expect(planRestore({ state: state(), launch })).toEqual([{
      sessionId: 'a1',
      title: 'ccfzf',
      command: 'wt.exe',
      args: ['-w', '-1', '-t', 'ccfzf --session a1 --kiosk'],
      bounds,
      desktop: 2,
    }]);
  });

  it('skips layout entries without a slot', () => {
    expect(planRestore({ state: state({ lastLayout: ['a1', 'gone'] }), launch })).toHaveLength(1);
  });

  it('returns nothing for an empty layout', () => {
    expect(planRestore({ state: state({ lastLayout: [] }), launch })).toEqual([]);
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npx vitest run src/claude-wt/restore-helpers.test.js`
Expected: FAIL — `Failed to resolve import "./restore-helpers.js"`

- [ ] **Step 3: Реализовать хелперы**

Create `src/claude-wt/restore-helpers.js`:

```js
/** Pure helper functions for claude-wt crash restore. No external I/O. */

function bootTimeSec(uptimeSec, nowMs) {
  return Math.floor(nowMs / 1000) - Math.floor(uptimeSec);
}

/**
 * A crash is a state file last written before the current boot while sessions
 * were still on screen. Closing the terminals by hand empties lastLayout on the
 * very next tick, so a surviving non-empty layout means the machine went down
 * without asking.
 */
function detectCrash({ state, bootTimeSec: boot, windowCount }) {
  if (!state?.updated) return false;
  if (!state.lastLayout?.length) return false;
  if (windowCount > 0) return false;
  return state.updated < boot;
}

function planRestore({ state, launch }) {
  return (state.lastLayout ?? [])
    .map(sessionId => ({ sessionId, slot: state.slots?.[sessionId] }))
    .filter(({ slot }) => Boolean(slot))
    .map(({ sessionId, slot }) => ({
      sessionId,
      title: slot.titles[0],
      command: launch.command,
      args: launch.args.map(arg => arg.replaceAll('{id}', sessionId)),
      bounds: slot.bounds,
      desktop: slot.desktop,
    }));
}

export { bootTimeSec, detectCrash, planRestore };
```

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `npx vitest run src/claude-wt/restore-helpers.test.js`
Expected: PASS, 9 тестов

- [ ] **Step 5: Коммит**

```bash
git add src/claude-wt/restore-helpers.js src/claude-wt/restore-helpers.test.js
git commit -m "feat(claude-wt): детект аварии и план восстановления"
```

---

### Task 8: Флаг `--session <id>` в ccfzf

Восстановление требует неинтерактивного входа в сессию по её id. Сейчас ccfzf принимает только проект и всегда показывает список. Работа идёт в **другом репозитории**: `~/projects/shell/ccfzf` (`~/bin/ccfzf` — симлинк туда).

Без этой задачи позиционная память работает, восстановление после краха — нет.

**Files:**
- Modify: `~/projects/shell/ccfzf/ccfzf` (разбор аргументов, ветка прямого запуска, блок usage в шапке)
- Modify: `~/projects/shell/ccfzf/README.md`

**Interfaces:**
- Consumes: ничего.
- Produces: `ccfzf --session <id>` — запускает `claude --resume <id>` в каталоге сессии; вместе с `--kiosk` по выходу возвращает в список сессий проекта этой сессии; вместе с `--print` печатает команду вместо запуска.

- [ ] **Step 1: Свериться с местами вставки**

Run: `grep -n "print_mode=0\|--print) print_mode\|^dump_state$\|# --- resolve the project argument\|# --- launch" ~/projects/shell/ccfzf/ccfzf`

Три ориентира: объявления флагов (`print_mode=0`, `kiosk=0`, `project=""`), цикл `while (($#))` с ветками `--print` и `-k|--kiosk`, и одиночный вызов `dump_state` перед комментарием `# --- resolve the project argument`. Новый код встаёт ровно в эти три места.

- [ ] **Step 2: Написать проверку, которая падает**

Run: `~/bin/ccfzf --session dad9f8b1-df6c-4bd9-91f2-5e4e3b822075 --print`
Expected: FAIL — неизвестный аргумент либо запуск интерактивного выбора вместо печати команды

- [ ] **Step 3: Разобрать новый флаг**

Рядом с `print_mode=0` / `kiosk=0` / `project=""` объявить:

```bash
session_id=""
```

В цикле `while (($#))` добавить две ветки перед строкой `-h|--help`:

```bash
    --session) session_id="${2-}"; [[ -n $session_id && $session_id != -* ]] || { echo "ccfzf: --session requires a session id" >&2; exit 2; }; shift 2 ;;
    --session=*) session_id="${1#*=}"; [[ -n $session_id ]] || { echo "ccfzf: --session requires a session id" >&2; exit 2; }; shift ;;
```

- [ ] **Step 4: Реализовать прямой вход в сессию**

Вставить блок сразу после одиночного вызова `dump_state` и перед комментарием
`# --- resolve the project argument`:

```bash
# --- direct entry into a session by id --------------------------------------
# Called by external automation (claude-wt restore), so a missing session is an
# error, not a reason to drop into the picker: that would hang the caller.
if [[ -n $session_id ]]; then
  sess_file=$(ls -1 "$HOME"/.claude/projects/*/"$session_id".jsonl 2>/dev/null | head -1 || true)
  [[ -n $sess_file ]] || { echo "ccfzf: session $session_id not found" >&2; exit 1; }
  # cwd не на первой строке транскрипта: в реальных файлах он появляется на
  # 3-4-й (проверено на 300 файлах — на строке 0 его нет ни в одном). Идиома та
  # же, что в собственной dir_cwd() ccfzf: просмотреть голову файла.
  dir=$(python3 -c 'import json,sys
try:
    with open(sys.argv[1], "rb") as fh:
        for _ in range(50):
            line = fh.readline()
            if not line:
                break
            try:
                o = json.loads(line)
            except Exception:
                continue
            if o.get("cwd"):
                print(o["cwd"].rstrip("/"))
                break
except OSError:
    pass' "$sess_file")
  [[ -n $dir && -d $dir ]] || { echo "ccfzf: session $session_id has no usable cwd" >&2; exit 1; }
  cmd="$CLAUDE_CMD --resume $(printf '%q' "$session_id")$claude_tail"
  cmd=${cmd% }
  if ((kiosk)); then
    (cd "$dir" && exec "$SHELL" -c "$cmd") || true
    rows=$(python3 -c "$PY" projects "$MARKS" "$index")
    all_rows=$rows
    dump_state
    picked=$(awk -F'\t' -v p="$dir" '$1 == p { print; exit }' <<<"$rows")
    [[ -n $picked ]] || picked=$(printf '%s\t%s\t%s' "$dir" "$(basename "$dir")" "$dir")
    query=""
    posbind=""
  elif ((print_mode)); then
    printf 'cd %q && %s\n' "$dir" "$cmd"
    exit 0
  else
    cd "$dir"
    exec "$SHELL" -c "$cmd"
  fi
fi
```

В kiosk-ветке блок не выходит, а оставляет `picked` заполненным — цикл
`while :;` начинается с `if [[ -z $picked ]]`, поэтому управление сразу
попадает в список сессий нужного проекта, ровно как при обычном возврате из
kiosk-запуска.

Переменные `picked`, `query`, `posbind` и `all_rows` объявляются ниже, в блоке
разрешения проекта. Перенести их инициализацию (`all_rows=$rows`, `picked=""`,
`query=$project`, `posbind=""`) выше — прямо перед вставленным блоком, иначе она
затрёт то, что блок выставил.

Обновить блок usage в шапке файла (строки 2-21, их печатает `usage()`), добавив
строку после строки про `--print`:

```
#   ccfzf --session <id>        go straight into a session by id, no picker
```

- [ ] **Step 5: Проверить вручную**

```bash
id=$(python3 -c "import json;print(json.load(open('$HOME/.ccfzf.sessions.json'))['sessions'][0]['id'])")
~/bin/ccfzf --session "$id" --print          # печатает команду с этим id, ничего не запускает
~/bin/ccfzf --session no-such-id --print     # сообщение в stderr, код возврата 1
echo $?
~/bin/ccfzf --session "$id"                  # входит в сессию
```

Expected: первая команда печатает `cd … && claude --resume <id>`; вторая — ошибку и `1`; третья открывает сессию.

- [ ] **Step 6: Обновить README и закоммитить**

Добавить `--session <id>` в список опций в `README.md`, затем:

```bash
cd ~/projects/shell/ccfzf
git add ccfzf README.md
git commit -m "feat: --session <id> for non-interactive resume"
```

---

### Task 9: Исполнение восстановления и команда `claude-wt restore`

Запуск последовательный, по одному окну: несколько одновременно всплывших окон не к чему привязать. Появившееся окно опознаётся по новому `id`, а не по заголовку — заголовок к этому моменту ещё не устоялся.

**Files:**
- Create: `src/claude-wt/restore.js`
- Modify: `src/claude-wt/index.js` (реэкспорт и автозапуск при `restore.auto`)
- Modify: `src/index.js` (подкоманда `claude-wt restore`)

**Interfaces:**
- Consumes: `planRestore`, `detectCrash`, `bootTimeSec` (Task 7); `getClaudeWtConfig`, `isTerminalWindow` (Task 6); `readState` (Task 2); `clampBoundsToMonitors` (Task 5).
- Produces:
  - `waitForNewWindow(knownIds, timeoutMs, pollMs) → Promise<window | null>`
  - `restoreClaudeSessions() → Promise<{ restored: string[], skipped: string[] }>`
  - `maybeRestoreOnStart() → Promise<boolean>` — проверяет признак аварии, восстанавливает только при `restore.auto`.

- [ ] **Step 1: Реализовать восстановление**

Create `src/claude-wt/restore.js`:

```js
import os from 'node:os';
import { spawn } from 'node:child_process';
import { getWindows } from '../windows.js';
import { placeWindowByConfig } from '../placement.js';
import { getWindowsMonitors } from '../monitors.js';
import { clampBoundsToMonitors } from '../geometry.js';
import { readState } from './state.js';
import { getClaudeWtConfig, isTerminalWindow } from './index.js';
import { bootTimeSec, detectCrash, planRestore } from './restore-helpers.js';

const sleep = ms => new Promise(resolve => setTimeout(resolve, ms));

/** The window we just launched is the one whose id was not there before. */
async function waitForNewWindow(knownIds, timeoutMs, pollMs = 500) {
  const deadline = Date.now() + timeoutMs;
  while (Date.now() < deadline) {
    await sleep(pollMs);
    const found = getWindows().filter(isTerminalWindow).find(w => !knownIds.has(w.id));
    if (found) return found;
  }
  return null;
}

async function restoreClaudeSessions() {
  const cfg = getClaudeWtConfig();
  const state = readState(cfg.statePath);
  const plan = planRestore({ state, launch: cfg.launch });
  const restored = [];
  const skipped = [];
  if (!plan.length) {
    console.log('[claude-wt] nothing to restore');
    return { restored, skipped };
  }
  const monitors = getWindowsMonitors();
  for (const item of plan) {
    const knownIds = new Set(getWindows().filter(isTerminalWindow).map(w => w.id));
    console.log(`[claude-wt] restoring ${item.title} (${item.sessionId})`);
    try {
      spawn(item.command, item.args, { detached: true, stdio: 'ignore' }).unref();
    } catch (e) {
      console.error(`[claude-wt] failed to launch ${item.sessionId}: ${e.message}`);
      skipped.push(item.sessionId);
      continue;
    }
    const win = await waitForNewWindow(knownIds, cfg.restore.windowTimeoutMs);
    if (!win) {
      console.error(`[claude-wt] window for ${item.sessionId} did not appear, skipping`);
      skipped.push(item.sessionId);
      continue;
    }
    const bounds = clampBoundsToMonitors(item.bounds, monitors);
    const rule = { window: win.id, ...bounds };
    if (cfg.desktop && item.desktop) rule.desktop = item.desktop;
    try {
      await placeWindowByConfig(rule);
      restored.push(item.sessionId);
    } catch (e) {
      console.error(`[claude-wt] failed to place ${item.sessionId}: ${e.message}`);
      skipped.push(item.sessionId);
    }
  }
  console.log(`[claude-wt] restored ${restored.length}, skipped ${skipped.length}`);
  return { restored, skipped };
}

/** Called on daemon start: report a crash, act on it only when configured to. */
async function maybeRestoreOnStart() {
  const cfg = getClaudeWtConfig();
  const state = readState(cfg.statePath);
  const windowCount = getWindows().filter(isTerminalWindow).length;
  const crashed = detectCrash({
    state,
    bootTimeSec: bootTimeSec(os.uptime(), Date.now()),
    windowCount,
  });
  if (!crashed) return false;
  console.log(`[claude-wt] ${state.lastLayout.length} session(s) were open before the reboot`);
  if (!cfg.restore.auto) {
    console.log('[claude-wt] run "node src claude-wt restore" to bring them back (restore.auto is off)');
    return false;
  }
  await restoreClaudeSessions();
  return true;
}

export { waitForNewWindow, restoreClaudeSessions, maybeRestoreOnStart };
```

- [ ] **Step 2: Вызвать проверку аварии при старте демона**

В `src/claude-wt/index.js`, в `startClaudeWt()`, после строки с `console.log('[claude-wt] watching...')` добавить:

```js
  import('./restore.js')
    .then(mod => mod.maybeRestoreOnStart())
    .catch(e => console.error(`[claude-wt] crash check failed: ${e.message}`));
```

Импорт динамический: `restore.js` импортирует `./index.js`, статический импорт в обратную сторону дал бы цикл.

- [ ] **Step 3: Добавить подкоманду CLI**

В `src/index.js`, рядом с остальными подкомандами `claude-wt`:

```js
  claudeWt.command('restore').action(async () => {
    const mod = await import('./claude-wt/restore.js');
    await mod.restoreClaudeSessions();
    process.exit(0);
  });
```

- [ ] **Step 4: Проверить, что ничего не сломалось**

Run: `npm run lint && npm test`
Expected: линт без ошибок, все тесты зелёные

- [ ] **Step 5: Проверить вручную на Windows**

Требует выполненной задачи 8.

1. Открыть две сессии в двух окнах, расставить их, дождаться, пока `node src claude-wt status` покажет оба слота и `lastLayout` из двух id.
2. Остановить демон, закрыть оба окна вручную (`lastLayout` останется от последнего тика с окнами — это ожидаемо, детект аварии здесь не участвует).
3. Run: `node src claude-wt restore`
4. Ожидается: окна открываются по одному, каждое встаёт на своё место; в конце строка `restored 2, skipped 0`.

Проверка детекта аварии — только естественным путём: при следующей внеплановой перезагрузке `node src claude-wt watch` должен напечатать `N session(s) were open before the reboot`.

- [ ] **Step 6: Коммит**

```bash
git add src/claude-wt/restore.js src/claude-wt/index.js src/index.js
git commit -m "feat(claude-wt): восстановление раскладки после аварийной перезагрузки"
```

---

### Task 10: Трей Tauri и документация

Демон должен включаться из трея, как автоплейсер, и быть описан в руководстве разработчика.

**Files:**
- Modify: `tauri-app/src-tauri/src/lib.rs` (пункты меню, обработчики, поле состояния)
- Modify: `AGENTS.md` (раздел про claude-wt, список команд CLI)
- Modify: `README.md` (строка в списке возможностей)

**Interfaces:**
- Consumes: команды `node src claude-wt watch` и `node src claude-wt restore` из задач 6 и 9.
- Produces: пункты трея «Start claude-wt» / «Stop claude-wt» и «Restore claude sessions».

- [ ] **Step 1: Найти места вставки**

Run: `grep -n "autoplacer" tauri-app/src-tauri/src/lib.rs`

Четыре точки: поля `autoplacer_running` / `autoplacer_child` в `struct AppState`, их инициализация в `.manage(Mutex::new(AppState { ... }))`, функция `toggle_autoplacer()`, создание пункта меню `auto_i` и ветка `"autoplacer"` в обработчике меню. Новый тумблер повторяет ту же схему.

- [ ] **Step 2: Добавить поля состояния**

В `struct AppState` после `autoplacer_child` добавить:

```rust
    claude_wt_running: bool,
    claude_wt_child: Option<tauri_plugin_shell::process::CommandChild>,
```

В `.manage(Mutex::new(AppState { ... }))` после `autoplacer_child: None,` добавить:

```rust
            claude_wt_running: false,
            claude_wt_child: None,
```

- [ ] **Step 3: Добавить тумблер**

Сразу после функции `toggle_autoplacer` добавить:

```rust
fn toggle_claude_wt(app: &tauri::AppHandle, state: &State<'_, Mutex<AppState>>) {
    let project_path = get_project_path(app);
    if project_path.is_empty() {
        warn!("Project path not configured, opening settings");
        open_settings_window(app);
        return;
    }

    let mut app_state = state.lock().unwrap();

    if app_state.claude_wt_running {
        if let Some(child) = app_state.claude_wt_child.take() {
            let _ = child.kill();
        }
        app_state.claude_wt_running = false;
        info!("claude-wt stopped");
    } else {
        let shell = app.shell();
        let result = shell
            .command("node")
            .args(["src/index.js", "claude-wt", "watch"])
            .current_dir(&project_path)
            .spawn();

        match result {
            Ok((_rx, child)) => {
                app_state.claude_wt_child = Some(child);
                app_state.claude_wt_running = true;
                info!("claude-wt started");
            }
            Err(e) => error!("Failed to start claude-wt: {}", e),
        }
    }

    let _ = app.emit("claude-wt-toggled", app_state.claude_wt_running);
}
```

- [ ] **Step 4: Добавить пункты меню и обработчики**

После создания пункта `auto_i` добавить:

```rust
            let claude_wt_i =
                MenuItem::with_id(app, "claude_wt", "Start claude-wt", true, None::<&str>)?;
            let claude_wt_restore_i = MenuItem::with_id(
                app, "claude_wt_restore", "Restore claude sessions", true, None::<&str>)?;
```

Добавить `&claude_wt_i` и `&claude_wt_restore_i` в список элементов при сборке меню — там же, где перечислен `&auto_i`.

В обработчике событий меню после ветки `"autoplacer"` добавить:

```rust
                    "claude_wt" => {
                        let state = app.state::<Mutex<AppState>>();
                        toggle_claude_wt(app, &state);

                        let running = state.lock().unwrap().claude_wt_running;
                        let text = if running { "Stop claude-wt" } else { "Start claude-wt" };
                        let _ = claude_wt_i.set_text(text);
                    }
                    "claude_wt_restore" => {
                        run_node_command(
                            app, &["src/index.js", "claude-wt", "restore"], "claude-wt restore");
                    }
```

Ветка `"autoplacer"` показывает, как именно пункт меню попадает в замыкание обработчика (через `move` и клон handle) — повторить ту же схему для `claude_wt_i`.

- [ ] **Step 5: Собрать**

Run: `cd tauri-app/src-tauri && . "$HOME/.cargo/env" && cargo build`
Expected: сборка без ошибок

- [ ] **Step 6: Обновить документацию**

В `AGENTS.md`:

- в разделе «Build commands» дописать `claude-wt watch|status|restore|clear` в список команд Node CLI;
- в разделе «Project layout» добавить строку про `src/claude-wt/` — «позиционная память окон Claude Code, см. `docs/specs/2026-07-31-claude-wt-design.md`»;
- в «Key lib exports» добавить `src/claude-wt/index.js` с экспортами `startClaudeWt`, `stopClaudeWt`, `claudeWtStatus`, `getClaudeWtConfig`;
- добавить абзац-предупреждение: номер виртуального стола читается только при привязке окна к сессии, потому что `virtualDesktop.GetWindowDesktopNumber()` спавнит `VirtualDesktop11.exe`; вызывать его в цикле раз в секунду нельзя.

В `README.md` в список «Features» добавить строку: `Remember and restore window position per Claude Code session (claude-wt)`.

- [ ] **Step 7: Финальная проверка**

Run: `npm run lint && npm test`
Expected: линт без ошибок, все тесты зелёные

- [ ] **Step 8: Коммит**

```bash
git add tauri-app/src-tauri/src/lib.rs AGENTS.md README.md
git commit -m "feat(claude-wt): тумблер в трее и документация"
```

---

## Известные ограничения реализации

Их стоит держать в голове при приёмке — это осознанные решения, а не недоделки:

- Номер виртуального стола запоминается только в момент привязки окна к сессии. Перенос окна на другой стол без смены заголовка запомнится лишь при следующем входе в эту сессию. Причина — запрет на спавн `VirtualDesktop11.exe` в горячем цикле.
- Позиция привязана к сессии, а не к проекту: новая сессия в том же проекте открывается там, где её оставит пользователь, и запоминается отдельно.
- Сессии старше 200 последних выпадают из дампа ccfzf и опознаются только по собственной истории заголовков — то есть лишь те, которые claude-wt уже видел.
- Заголовок, который не удалось сопоставить с сессией, попадает в лог только при
  `claudeWt.debug: true`. Иначе смена каталога в шелле (заголовок меняется на
  каждый `cd`) залила бы лог строками про заголовки, которые сессиями и не были.
- `lastLayout` сохраняет состав последнего тика с окнами; при ручном закрытии всех окон при живом демоне он опустеет, при остановленном демоне — останется от последнего тика. На детект аварии это не влияет (там дополнительно проверяется `updated < boot time`), но `claude-wt restore` в таком случае поднимет сессии, закрытые вручную.

## Открытые вопросы, унаследованные задачами 6 и 9 — закрыты

Выявлены при исполнении задач 1-5, 7, 8 и финальном ревью, решены при написании
демона (задачи 6 и 9).

- **Троттл предупреждений в `sessions.js` не закреплён тестом.** Закрыт: два
  теста в `sessions.test.js` («warning throttle») на свежей копии модуля —
  три вызова подряд дают одно предупреждение, через шесть минут допускается
  второе.
- **Виртуальный стол восстанавливался только попутно с переносом.** Закрыт:
  `desktopOnlyActions()` в `daemon-helpers.js` выдаёт правило только со столом
  для окна, которое привязалось к сессии на этом тике и не получило своего
  `action`. Окна, впервые увиденные на этом тике, пропускаются — это случай
  перезапуска демона, растаскивать по столам всё открытое он не должен.
- **Успешный зажатый перенос записывал зажатую позицию.** Закрыт без нового
  поля в состоянии: `step()` не переписывает `bounds`, когда окно стоит ровно
  там, куда запомненная позиция ложится на текущие мониторы. Любая другая
  позиция считается выбором пользователя и записывается как раньше.
- **Сопоставление заголовков точное.** Оказалось не гипотезой, а живой поломкой:
  окно показывает `✳ ccfzf` (U+2733, статус-глиф Claude Code), а дамп хранит
  `ccfzf` — не совпало бы никогда. Закрыт `title-helpers.js`: обе стороны
  сравнения нормализуются одной функцией. Заодно снялось мигание глифа, из-за
  которого заголовок считался бы новым при каждом старте и остановке работы.
  Диагностика на остаток случаев — `claudeWt.debug: true`, по одной строке на
  несопоставленный заголовок.
- **Дедупликация записи состояния не работала как написана.** Закрыт:
  `layoutFingerprint()` сравнивает только `slots` и `lastLayout`, без `updated`.
