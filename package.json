{
  "name": "windows11-manager",
  "version": "4.1.0",
  "description": "Rules based manager of windows state for Windows 11: position, desktop, pin",
  "main": "src/lib/index.js",
  "bin": "src/index.js",
  "dependencies": {
    "commander": "^14.0.3",
    "mqtt": "^5.15.2",
    "yaml": "^2.9.0"
  },
  "optionalDependencies": {
    "node-window-manager": "file:./vendor/node-window-manager",
    "wql-process-monitor": "^3.0.2"
  },
  "scripts": {
    "version": "node scripts/sync-tauri-version.js && npm run changelog && git add CHANGELOG.md tauri-app/package.json tauri-app/src-tauri/tauri.conf.json tauri-app/src-tauri/Cargo.toml tauri-app/src-tauri/Cargo.lock",
    "build": "npm run version-sync && npm run build --prefix tauri-app && node -e \"import('fs').then(fs=>fs.cpSync('tauri-app/src-tauri/target/release/bundle','dist',{recursive:true}))\"",
    "start": "node src",
    "version-sync": "node scripts/sync-tauri-version.js",
    "dev": "npm run tauri dev --verbose --prefix tauri-app",
    "test": "vitest run",
    "lint": "eslint src/",
    "changelog": "git-cliff --unreleased --tag v$npm_package_version --prepend CHANGELOG.md"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "git-cliff": "^2.13.1",
    "globals": "^17.3.0",
    "vitest": "^3.0.0"
  },
  "author": "",
  "license": "ISC",
  "type": "module"
}
